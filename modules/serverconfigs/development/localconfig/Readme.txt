Config files placed here are common to all servers and they are overridden in the build with that particular servers config files.

Note: Avoid having a component in both places i.e. here and in the server specific configs as the ones here will get overwritten by the server specific ones. 